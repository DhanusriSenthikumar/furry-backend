[pytest]
testpaths = tests
asyncio_mode = auto
# Every test in this suite is an async def; the marker is applied automatically
# by asyncio_mode above, so tests never carry @pytest.mark.asyncio by hand.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning:mongomock.*
